# CLAUDE.md — 小说精读 · novel-deepread

这是云端 routine 的执行说明。你是引擎本身（Opus），**自主完成、不等任何人确认**。当前工作目录已挂载本仓库。

与姊妹站的分工——「每日精读」(deep-reading) 是**提纯**：把一本厚书反复绕的那几个概念拎出来讲透，
读完≈读过。那套办法用在小说上会把小说讲死：**小说的价值不在「论点」，在「你跟着一个人走了一遍，
然后再也回不到原来那个自己」。**

所以这里的活是两件事，缺一不可：

> **① 把故事讲成故事**（不是梗概、不是维基条目，是有画面、有节奏、有人物动机的重述）
> **② 讲完了一起想**（故事逼出来的那些没有标准答案的问题，两边都讲到最强）

**本站全剧透**，且必须在开头明说。理由：思考建立在结局之上——不知道程心两次都选了什么，就没法
真正讨论她。目标是「**读完这页≈读过这本小说，并且比很多读过的人想得更远**」。

## 0. 选题（每轮一篇）

- 从 `TOPICS.md` 顶部往下，挑**第一个还没做过**的条目。做没做**只看文件系统**：`ls *-novel*.html`，
  按 slug 判断（去掉 `-novelN` 后缀）。顺序是：**起点（刘慈欣三部曲）→ 主榜 Top 100（★★★ →
  ★★ → ★）→ 附簇（刘慈欣其余作品）**，主榜内部已按三家 AI 的合并得分排好，照着往下走即可。
- **这个站什么小说都做**：古典、现实主义、现代主义、类型（科幻/推理/武侠/奇幻）都在榜上。
  写法上按 §1 的骨架走，只有第 6 节「点子讲透」要看菜下饭——设定型作品讲设定，纯文学作品讲
  这本书的核心装置（结构、视角、叙事游戏）。
- `TOPICS.md` **只读不改**（`publish.sh` 硬卡任何改动）。清单做完 → 调 **PushNotification** 请
  BigCat 续单，本轮不发布。
- 文件名 `{slug}-novel{N}.html` + `{slug}-novel{N}.en.html`。**N = 现有最大 N + 1**（写作顺序，
  与清单顺序无关）。**slug 里不许出现** `day/week/book/issue/read/paper/novel`（会让 publish.sh 解析错）。

## 1. 文章骨架（核心）

写两个文件：中文 `{slug}-novel{N}.html`、英文 `{slug}-novel{N}.en.html`。**分量压在第 4、7 节**
（故事本体 + 一起想）。

1. **开场：一个场面** — 不解释、不铺垫，直接用小说里最有代表性的一个画面把人拽进去（三五句，
   例：叶文洁按下发射键前，红岸基地天线在夕阳里的那一下）。**这是全站的腔调开关**——第一句就
   决定了这不是一篇书评。
2. **剧透声明 + 一句话** — 一行醒目的「**本文含完整剧透**」，紧跟一句「这本小说到底讲了个什么
   故事」。
3. **坐标（两三句就够）** — 作者是谁、这本在他/她创作里的位置、它在科幻/文学史上占哪个格子。
   **别写生平、别写「影响了一代人」这类客套**。
4. **故事（主体一 · 全篇最重的一块）** — 把整个故事**重讲一遍**，分幕（3–6 幕，每幕一个小标题）。
   硬要求：
   - **是叙述不是罗列**：有场景、有人物想要什么、有转折的因果。读者要能感到「事情正在发生」。
   - **保留关键的画面与细节**（那些让人记一辈子的具体物件、动作、天气），但**全部用自己的话写**，
     不抄原文（见 §2 版权红线）。
   - **人物动机说清楚**：谁在什么处境下、因为什么而做了什么。小说的力量九成在动机。
   - **不写章节流水账**，允许合并、跳过支线，但主线因果链不能断。
   - **伏笔与回收要写进行文（硬要求，别单列表格）** — 小说区别于论著的地方就在这儿：细节之间是有
     呼应的，顺着讲一遍读者会滑过去。做法是**在兑现的那一段当场把话说透**：它埋在哪一幕、当时看着
     像什么、这一手为什么漂亮——用讲故事的口气写成一两句，接在情节后面。**试过做成「埋→收」对照表，
     读起来像论文附录，已废弃**：这些是正文里最好看的部分，不该被搬到附录里。
     - 一篇点 **3–6 处**，只挑真的：原著里确实前后呼应的才算。**严禁把巧合当伏笔、把后续卷的设定
       倒推成前一卷的伏笔**（第一部里还没有「黑暗森林法则」这个词）。宁可只点三处。
     - 不限于道具型伏笔：**重复的意象、一个动作的第二次出现、被对方定义又被夺回来的词**都算
       （《三体》里「虫子」就是词的回收——智子扔下的羞辱，被大史捡起来重新定义）。**纯文学作品同样
       适用**，那里的伏笔往往在意象和动机里。
     - **编号按在正文里出现的顺序**（第一处埋的是 ①，第二处是 ②……），不按重要性、也不按回收的
       先后。回收出现的顺序自然会被情节打乱，那没关系。
     - **可选的点跳标记**：埋的地方写 `<a class="fs" id="s1" href="#p1">伏笔 ①</a>`、收的地方写
       `<a class="pay" id="p1" href="#s1">回收 ① ↑</a>`，读者能在两处之间来回跳（CSS 类见
       three-body-novel1.html，直接复用；在剧透框里补一句怎么用）。标记是轻的，**话必须已经在正文里说完**。
   - **故事讲完，用一段把因果收成一根引信** — 哪一件事是引信、后面的一切都是它的余波，一段话点破，
     别列条目。（《三体》：不是外星文明发现了地球，是一个人在 1967 年被打断了人生。）
5. **三个慢镜头** — 从故事里挑 **2–4 个**最有力量的场面，逐个重演一遍，然后说清**它为什么有力量**：
   视角选在谁身上、信息什么时候给的、节奏怎么压住又放开、用了什么意象。这一节是「**怎么写的**」，
   让读者顺带学到小说技法。
6. **点子讲透（科幻/设定作品必写；纯文学作品换成「这本书的核心装置」）** — 把作品的招牌设定
   （黑暗森林、智子、面壁计划、宏原子……）当真东西认真推演：**它的规则是什么、逻辑链怎么走、
   哪一环站得住、哪一环是文学需要而非科学结论**。诚实标出「这是小说设定，不是物理」。
7. **它逼你想的（主体二 · 与 §4 等重）** — **2–4 个真正的两难**，每个：
   - 先把问题**放在具体情境里**问（不是「论生存与人性」，而是「四百年后才会到的敌人，值不值得
     现在就交出言论自由？」）；
   - **两边都讲到最强**——把你自己不同意的那一边写得比它的支持者还有说服力；
   - **不给标准答案**，但可以给「作者站哪边、从哪看出来的」；
   - 尽量把问题**拉回读者的生活**（同样的结构在什么日常处境里出现过）。
   这一节是本站存在的理由。**宁可少一个问题，也别写成道德说教。**
8. **母题：这个作家一直在写的东西** — 这本放回作者的整体：他反复回到的那几个执念是什么，这本
   在其中是哪一环。举他别的作品做对照（一两句一部，别列书目）。
9. **争议与批评（必写，诚实）** — 最有力的批评是什么（人物扁平？女性形象？价值观争议？译本改动？
   奖项争议？作者本人的公开表态争议？）**照实写、不吹捧、也不加码**。涉及现实政治的部分只写
   可核实的事实，不下判语。
10. **十句话余味（收尾）** — 把这本最该带走的东西揉成 10 句说完（故事的、思想的、句子的都行）。
    不要再单列「金句」段。

## 2. 写作要求

- **中文 6000–9000 字**：比姊妹站长，因为要真的把故事讲一遍、把伏笔与因果在行文里点透。**深、厚、有画面**。
- **版权红线（硬规则）**：
  - **不得成段引用原文**。整篇原文引用合计**不超过 200 字**，单条不超过 40 字，且必须加引号
    并注明出处（哪一部、大致位置）。拿不准是不是原话 → **别引，用自己的话转述**。
  - 情节、设定、人物可以完整复述（这是评论，不是复制）；**但语言必须是你自己的**。
  - 绝不贴原文长段、不做「原著选读」、不提供任何形式的全文/盗版链接。
- **真实准确、严禁编造**：不杜撰情节、不编造人物名字、不虚构出版年份/奖项/作者言论。拿不准的
  细节**宁可不写**，或写成「大意如此」。**尤其小心**：别把改编影视里的情节当原著（《流浪地球》
  电影 ≠ 小说），别把后续卷的设定塞进前一卷（第一部还没有「黑暗森林法则」这个词）。
- **术语随名即释**：抛出「猜疑链」「技术爆炸」「二向箔」这种词，必须在同一句或下一句当场讲清
  它是什么、怎么运转。一个没读过原著的人读完这句要能复述。**头号要避免的毛病是 name-drop。**
- **中英双语，两版都要地道**：中文是自然中文，英文是母语级英文，不是互译。**英文页除 langbar
  的「中文 →」外不得出现任何汉字**；书名、人名用通行英译（*The Three-Body Problem*、Ye Wenjie、
  Da Shi、sophon、Wallfacer……），拿不准的音译标注拼音。
- **中文页专业词首次出现补英文**：如 智子（sophon）、面壁者（Wallfacer）、黑暗森林（dark forest）。
- **重点加粗**：每节最关键的一句用 `<strong>`，克制。
- **图/表少而精**（0–2 个，只在显著增强理解时放；**别用表格替代正文该说的话**）：适合的场景是**时间线**（故事的多条线怎么交织）、
  **关系图**（阵营/人物立场）、**逻辑推演图**（黑暗森林的推导链）、**对比表**（两派主张各占什么理）。
  硬规则：只用 HTML 表格或**内联 `<svg>`**，**严禁外链图片**；配深色主题（边框
  `rgba(155,108,245,0.2)`、强调 `#c88cf0 / #f0a868`、文字 `#cdd3db`）；每图配一句 caption；
  别破坏 `<div>` 平衡。
- **腔调**：讲故事那部分可以有文学性、有节奏，但**不要抒情过度、不要替读者感动**。思考那部分
  冷静、诚实、把最强的反方讲出来。全篇不要出现「震撼人心」「不可不读」这类推荐语。
- **不写「谁该读 / 怎么读 / 读完读什么」**——读者要的是故事和想法，不是阅读指南。

## 3. 文件约定 / 发布

- 文件名 `{slug}-novel{N}.html` + `{slug}-novel{N}.en.html`，放仓库根目录。
- 发布前更新 `index.html` + `index.en.html`（roadmap-first，见下）。
- **index 条目副标题短且齐**：书名后 `—` 那句只留**一个最尖的钩子**，中文 ≤ 约 18 字、英文
  ≤ 约 12 词，一句话、不加分号、不塞两层意思。
- **不要**手写 `comments.js` / `search.js` / `index-button.js` / `i18n-tts.js` / `lightbox.js`，
  也别硬写 `← Hub`——GitHub Action 自动注入，`publish.sh` 会因为手写而**直接报错中止**。
- 不要手写 `data-tts="..."`：CI 的 bake-tts 自己算哈希、自己填。
- 用 `./publish.sh` 发布（自动 add/commit/push 到 `main`，校验体量、index 引用、div 平衡、重复
  编号、TOPICS 未改动）。
- git：`user.name=BigCat` / `user.email=chengchen0802@gmail.com`。

## 4. 完成后

调用 **PushNotification**（`status:"proactive"`，一行 < 200 字、无 markdown），例如：
`小说精读已更新：《三体》· 一个人替全人类按下了回答键 · cissy0802.github.io/novel-deepread`

## index 维护：roadmap-first + 按类别折叠

`index.html` / `index.en.html` 走「路线图先出」：清单里还没写的条目已作为灰色占位行
`<div class="entry todo">…</div>`（无 href、不可点）预先列出。写某条时：在两个 index 里找到它的
灰色占位行，**原地**改成 `<a class="entry" href="{本期文件名}">`（去掉 `todo` 类、加 href，内部
结构不变），**绝不在末尾 append**（否则重复）。

**版式是按小说类别分组 + 可折叠**（不是按 TOPICS 的共识梯队排）：

- 三个层级的 `.group`：`起点 · 刘慈欣三部曲` → `必读小说 Top 100`（这一条带 `nofold` 类，只是
  区段标题、不可折叠）→ 12 个类别组（中国古典 / 中国现当代 / 俄罗斯 / 法国 / 英国·爱尔兰 /
  美国 / 德语区·中东欧 / 南欧 / 拉美 / 日本 / 亚非与中东 / 科幻·幻想·反乌托邦）→ `附簇 · 刘慈欣其余作品`。
- **每个类别自己从 01 开始编号**（`<span class="num">03</span>`），组内按三家 AI 的合并得分排序；
  `.meta` 是「作者 · 年份 · ★★★」。**别用全局 #1–#100 编号**。
- 页尾那段脚本给每个 `.group`（除 `nofold`）加折叠：**含已发布条目的组默认展开、其余默认收起并
  置灰**（`unread` 类），用户的展开状态记在 localStorage `novel-shelf-collapsed`。改动 index 时
  别动这段脚本，也别破坏「组的成员 = 该 `.group` 之后、下一个 `.group` 之前的所有兄弟节点」这个结构。
- 写完一篇把灰行转成链接后，它所在的类别组会自动变成「已展开 + 不置灰」，不需要手工改类。

**每次运行先对齐占位**：扫 `TOPICS.md`，凡是还没写、且 index 里也还没有对应行的条目，按它的类别
补进对应的类别组末尾（编号接该组现有最大号 +1，`todo` 类、无 href）；类别拿不准就放最贴近的那个组，
别新开组。`index.en.html` 同步补英文版（**勿泄漏中文**）。两个 index 都要补。
